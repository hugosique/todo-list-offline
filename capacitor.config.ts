import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.todo-list-offline.app',
  appName: 'todo-list-offline',
  webDir: 'www'
};

export default config;
